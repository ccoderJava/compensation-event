spring:
  application:
    name: compensation-event-test
  cloud:
    consul:
      host: ${CONSUL_HOST:127.0.0.1}
      port: ${CONSUL_PORT:8500}
    loadbalancer:
      ribbon:
        enabled: true